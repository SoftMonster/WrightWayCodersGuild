A really cool Chat GPT prompt:

Generate a single self-contained HTML file that runs entirely offline and displays a dynamic directory interface built from a JavaScript array named tools.
The file must contain only embedded style and script blocks, use no external libraries, and rely entirely on vanilla JavaScript with DOM-based rendering.
The tools dataset is the only source of truth for the interface.
Each entry contains an id, emoji, name, description, category, and url.
The system must render all entries dynamically at runtime, treat each entry as a fully clickable card linking to its url,
and ensure all ordering, filtering, and counts are computed directly from the dataset without hardcoded values.
All items must be sorted alphabetically by name before rendering.
Categories must be derived automatically from the dataset, normalized in a case-insensitive manner, and deduplicated before being used to build filter controls.
A default “All” view must always exist and be selected on load.
Selecting a category must filter visible cards and update a live count of displayed items.
The interface must be built entirely through DOM operations in JavaScript, using document.createElement and appendChild,
with innerHTML only used in limited templating where necessary.
Rendering must occur on page load and must be deterministic so that identical input always produces identical output.
The visual design system must be abstract rather than fixed.
A theme is defined as a set of parameters that control background style, surface styling, accent behavior, contrast rules, and motion intensity.
Themes may vary across implementations, such as minimal, cyber, neutral, or domain-specific visual identities.
Regardless of theme, readability and functional clarity must remain the priority over decorative effects.
The layout must be responsive and centered, with a grid-based arrangement of cards that adapts fluidly to screen size and maintains a minimum card width
of approximately 280 pixels. Each card must present an emoji, a name, a short description, and a category label.
The entire card surface must act as a single interactive link that opens in a new tab.
The interface must include a header with a title, a dynamic subtitle reflecting the total number of items in the dataset, a category filter bar generated from the data,
a grid container for the cards, and a footer showing a runtime-generated timestamp.
All state changes must be reflected immediately in the interface without page reloads.
Filtering must be based on case-insensitive category matching and must always update both visible items and the displayed count.
Active filters must be visually indicated in a stable and consistent manner.
The system must remain lightweight, fully offline-capable, and free from external dependencies, ensuring predictable rendering and behavior across environments.
To populate this page, use a curated set of the most widely recognized and actively used resources within the chosen domain,
selecting entries that collectively represent the most relevant, high-signal, and commonly referenced items in that space.

Your chosen theme for this task is url cards for: Top <n> most popular <topic> sites
